{
  "name": "speechace-txt-root",
  "private": true,
  "scripts": {
    "postinstall": "cd client && npm i",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "busboy": "^1.6.0"
  }
}
