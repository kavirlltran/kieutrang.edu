@'
{
  "name": "speechace-txt-root",
  "private": true,
  "scripts": {
    "build": "npm --prefix client run build"
  },
  "engines": {
    "node": ">=18"
  }
}
'@ | Set-Content -Path .\package.json -Encoding utf8
